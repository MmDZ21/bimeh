import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

import React from "react";

const Alarms = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Alarms</CardTitle>
      </CardHeader>
      <CardContent>AlarmList</CardContent>
    </Card>
  );
};

export default Alarms;
