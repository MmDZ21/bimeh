import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const RecentClients = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>RecentClients</CardTitle>
      </CardHeader>
      <CardContent>Clients List</CardContent>
    </Card>
  );
};

export default RecentClients;
